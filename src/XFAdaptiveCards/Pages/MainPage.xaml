<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:XFAdaptiveCards.Enums"
             mc:Ignorable="d"
             x:Class="XFAdaptiveCards.Pages.MainPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center"
                     HorizontalOptions="FillAndExpand"
                     Padding="50">
            <Button Text="Flight Itinerary"
                    Command="{Binding ButtonClickedCommand}"
                    CommandParameter="{x:Static local:AdaptiveCards.FlightItenerary}"/>

            <Button Text="Food Order"
                    Command="{Binding ButtonClickedCommand}"
                    CommandParameter="{x:Static local:AdaptiveCards.FoodOrder}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>